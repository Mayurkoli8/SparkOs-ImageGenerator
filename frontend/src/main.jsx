import React from "react";
import ReactDOM from "react-dom/client";
import SparkOs from "./SparkOs.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SparkOs />
  </React.StrictMode>
);
