services:
  - type: web
    name: sparkos-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: DEFAULT_IMAGE_MODEL
        value: gpt-image-1
      - key: BASE_URL
        sync: false
      - key: FRONTEND_URL
        value: "*"
      - key: DATA_DIR
        value: /var/data
    disk:
      name: sparkos-storage
      mountPath: /var/data
      sizeGB: 10

  - type: web
    name: sparkos-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
